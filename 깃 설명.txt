Git 용어 설명 ------------------------------------------------

git checkout     깃의 코드 번호 입력 < 사용한다.
git checkout -   가장 최근의 저장한 깃 으로 돌아간다. 
git add  파일명  < 스테이지에 올린다. 
git commit -m "설명을 입력한다"
git log 

push  < 올린다
pull    < 내려받는다 변경된것만 
clone  < 전체 내려받기 (순수한 빈폴더에 전체 다운받기 개념) 

소스트리 
내 로컬과 원격 저장소에 연결해주는것 
- 호스트 계정 편집 > 계정추가 
- GitHub 변경
- BASIC 으로 연결 
>> 계정등록 성공하면 원격저장소 목록 보임 
= 성공할 경우 예시  
